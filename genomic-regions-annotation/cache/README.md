# Chromatin model cache

Downloaded and preprocessed dense BED models are stored here.
This directory is gitignored. Only final `{collection}_{genome}_dense.bed` files (and `.model_meta.json` sidecars) should remain.
