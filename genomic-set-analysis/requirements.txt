# Requires Python 3.8-3.9: Intervene 0.6.4 imports collections.Iterable (removed in 3.10+).
# Prefer the persistent cache env (not recreated each run):
#   bash scripts/ensure_env.sh
# Location: ~/.cache/ai-skills-env/genomic-set-analysis/conda-env/
# Rebuild:   bash scripts/ensure_env.sh --force-rebuild
#
# Core overlap (intervene_peaks_combine.py)
pybedtools>=0.9.0
pandas>=1.3.0
numpy>=1.21.0
# Expression summaries (expression_summary.py)
scipy>=1.7.0
seaborn>=0.12.0
matplotlib>=3.5.0
# Chained pathway enrichment (pathway-enrichment-enrichr skill; SKILL.md Step 3).
# Install these in the same env as the overlap step so Enrichr Excel export and API
# calls succeed without switching conda envs mid-workflow.
xlsxwriter>=3.0.0
requests>=2.28.0
rich>=13.0.0
# Testing
pytest>=7.0.0
#
# External binaries (install separately, not via pip):
#   intervene   ->  conda install bioconda::intervene
#   bedtools    ->  conda install bioconda::bedtools
